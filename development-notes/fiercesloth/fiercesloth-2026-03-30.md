# Дата: 2026-03-30

## Что было сделано (включая 26-30 марта):

1. Дизайн страницы `TheoryHub`
2. Верстка страницы `TheoryHub`
3. Исправление замечаний с ревью

## Решения:

Взялся за задачу по виджету `TheoryHub` - это виджет, который будет в самом начале каждого модуля в роадмапе.

Сначало взялся за дизайн и типы данных которые он использует. Потом сделал каркас добавив репозиторий для работы с апи, но пока бэкенда нету, и делаю все на моковых данных, затем добавил данные и сразу маппер для перевода `DTO` типов в `Entity`.

Сделал всю вертску страницы и базовку логику. Виджет идет как страница с заголовком и описанием, а затем с красивым списком ресурсов. Так же добавил спиннер с помощью `LoaderManager`, когда страница загружается.

Поправил все правки с ревью `CodeArena` и полностью смержил его.

## Планы:

Для `TheoryHub` осталось подключить бд, но для этого все уже сделано, и добавить 2 action кнопки, это `Back to Roadmap` и `Confirm` которые оба переходят на страницу `Roadmap`, но второй также сохранит прогресс за этот виджет.

## Затраченное время: 12 часов (за 26-30)
