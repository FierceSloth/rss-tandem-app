# Дата: 2026-03-12

## Что было сделано:

1. Завершил работу над компонентом `Spinner`.
2. Завершил работу над компонентом `Toast`.
3. Добавил комментарии к коду.
4. Завершил тикет `RSS-TD-14`. [PR](https://github.com/FierceSloth/rss-tandem-app/pull/39)

## Решения:

Полностью закончил верстку страницы `Roadmap` и так же добавил красивые ховер анимации для карточек. На самой странице осталось лишь подключить бэкенд, сделать `Spinner` загрузчик и выводить сообщения ошибки в try catch. Для всего этого вчера я сделал глобальные компоненты `Toast` и `Spinner`.

Стили для них реализовал через ИИ, что бы они не были скучными. Теперь они имеют красивые стили и анимации подходящие под наше приложение.

Сегодня я должен был интегрировать их в страницу `Roadmap`, но решил взять сегодня небольшой перерыв.

## Проблемы:

Для страницы `Roadmap` еще не готов бэкенд, поэтому это еще одна причина, почему я не добавил `Toast` и `Spinner` для страницы.

## Планы:

Планирую заврешить страницу `Roadmap` когда будет бэкенд. Думаю что будет это через 1-3 дней.

На созвоне во вторник взял себе новый тикет на 4 спринт. Это страница `TaslResolver` (аналог CodeWars). Взял на него 3-4 дня. Думаю что не будет его сложно реализовать, ведь основную логику взаимодействия `CodeEditor` и `Terminal` я прописал в `ExecutionController` специально для таких виджетов.

## Затраченное время: 3 часа
