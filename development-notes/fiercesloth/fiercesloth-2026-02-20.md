# Дата: 2026-02-20

## Что было сделано:

1. Написан гайдлан о стандартах написания компонентов
2. Добавлен vscode сниппет, для автоматического подставления базовой стуктуры комопнентов
3. Проведен созвон с командой

## Решения:

В корень проекта добавил сниппет по совету ии, и написал о том как его использовать в гайдлайне. Так же в сегодняшнем мите мы с командой обсуждали дальнейшие действия и распределили следующие задачи. Решили пока что делать все данные моковыми, а потом добавлять бэкенд через firebase или supabase. Но еще не решили что конкретно будем использовать.
На мите я взял задачу сделать глобальный компонент `CodeEditor` и написать `TestEngine` для проверки кода внутри него. Вчера изучал варианты реализации редактора и решил использовать CodeMirror вместо Monaco, так как Monaco будет слишком тяжеловесным для нашего проекта.

## Проблемы:

Во время написания сниппета была проблема, в которой VS Code автоматически не давал подсказку при написании кодового слова `view`. Оказалось, что папка `.vscode`, в которой был сниппет, находилась в папке `rss-tandem`, а в самом проводнике у меня была открыта папка `RS School`, в которой были все репозитории, и чтобы сниппет работал, он должен находиться в корне открытой папки проводника, то есть в корне `RS School`.

## Планы:

Сделать глобальный компонент `CodeEditor`

## Затраченное время: 3 часа
