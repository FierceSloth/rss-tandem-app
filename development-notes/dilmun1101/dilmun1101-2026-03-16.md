# Дата: 2026-03-16

## Что было сделано:

Добавлена авториция с supabase, сделаны тесты и ревью

## Проблемы и решения:

Сегодня впервые писала тесты раньше никогда этого не делала. Часть тестов мне помог создать ии, он показал как вообще их делать. Я частично написала тесты для контроллеров, но они не прошли через ESLint. Там нужно было добавить отдельные правила линтера под тестовые файлы, но не стала этого делать самостоятельно, потому что такие вещи нужно сначала обсудить с командой. В итоге удалила проблемные файлы с тестами.

[Пример тестов](https://github.com/FierceSloth/rss-tandem-app/blob/develop/src/pages/auth-page/test/register-form.validator.test.ts)

Также добавила интеграцию с Supabase делала по видео, шаг за шагом. В процессе выяснилось что у меня был изменён путь к roadmap. Из-за этого при пуше сборка падала. Ещё по договорённости с командой нужно было добавить LocalStorage сохранение данных авторизации, чтобы роутер понимал авторизован пользователь или нет. Добавила токен в LocalStorage сразу после успешного входа. Но оказалось что формат записи токена отличался от того, что ожидал роутер. Пришлось немного повозиться. Разобраться какой именно формат ожидается.

#### Ревью

[feat: RSS-TD-08 add quiz page markup, timer, score result and other logic](https://github.com/FierceSloth/rss-tandem-app/pull/45)

`if (medalSvg) {
      this.view.icon.node.append(createSvgComponent(medalSvg));
    } else {
      this.view.icon.setText('💡');`

> > > Nice fallback. Using an emoji as a default icon is a clean UX touch.
> > > `medalSvg` has implicit type any here. Declare it explicitly: let medalSvg: string | null = null;. = null - the initial value, so that the variable is not undefined until it is first assigned a value

` private formatSeconds(totalSeconds: number): string {
const hours = Math.floor(totalSeconds / SIXTY_MINUTES_IN_SEC);
const remainder = totalSeconds % SIXTY_MINUTES_IN_SEC;

    const minutes = Math.floor(remainder / SIXTY_SECONDS);
    const seconds = remainder % SIXTY_SECONDS;

    return hours ? this.formatTimerWithHours(hours, minutes, seconds) : this.formatTimer(minutes, seconds);

}`

> > > The logic makes sense. It’s a good read.

## Планы

- Сделать рефакторинг кода.

## Затраченное время: 11 час
