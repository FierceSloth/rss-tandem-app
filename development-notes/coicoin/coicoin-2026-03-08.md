# Дата: 2026-02-08

## Что было сделано

- Запушил фикс на `Landing Page`, включающий корректное скрытие code editor при размере экрана < 768px.
- Локально добавил `progressBar` и`QuizFooter` в `Quiz page`, проработал архитектуру страницы.
- Продумал распределение компонентов на `ui`, `layout` и `features` и структуру контроллеров.

## Проблемы

- `Quiz page` сильно отличается по дизайну и функционалу от `Landing Page`.
- Сложности с адаптацией прогресс-бара и навигации между вопросами.

## Решения

- Решил использовать `QuizController` для хранения состояния: текущий вопрос, общее количество вопросов, логика перехода и проверки существования вопроса.
- Контроллер централизует данные для `progressBar`, `QuizHeader` и `QuizFooter`, обеспечивая правильный расчёт прогресса и навигацию.

## Планы

- Продолжить работу над страницей `Quiz` в рамках 3–4 спринта: интеграция вопросов, прогресс-бара, навигации и адаптивного дизайна.

## Затраченное время

8 часов
