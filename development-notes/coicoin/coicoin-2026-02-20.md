# Дата: 2026-02-20

## Что было сделано:

- Продолжил тестирование роутера и исправление багов с queries.
- Добавлен функционал корректного формирования URL с query (location.pathname + location.search).
- Созвонились с командой обсудили задачи на следующию неделю.
- Добавил тесты.

## Решения:

- добаботал функционал получение query /quiz/1?level=1, добавлением `location.pathname + location.search`.

Пример использования:

```ts
  public render(): void {
    const { id } = useParams();
    console.log(id);

    const quiz: Component = new Component({ className: styles.quiz });
    quiz.setText(JSON.stringify(useParams()));

    layout.root.append(quiz);
    useNavigate(ROUTES.TRUE_FALSE_PAGE, { id: '1' }, { level: '2', mode: 'junior' });
  }
```

Получится `http://localhost:7777/tandem/true-false/1?level=2&mode=junior`

- Добавил тесты для проверки правильной работы кода, защиты от ошибок при будущих правках

```ts
describe('Handle route, params and query', () => {
  it('should match dynamic route and extract params', () => {
    const { params } = matchRouteAndExtractParameters('/quiz/42', routes) ?? {};

    expect(params).toEqual({ id: '42' });
  });

  it('should return null if route does not match', () => {
    const result = extractParameters('/quiz/:id', '/quiz');

    expect(result).toBeNull();
  });

  it('should extract query parameters', () => {
    const query = extractQuery('level=2&mode=hard');

    expect(query).toEqual({
      level: '2',
      mode: 'hard',
    });
  });

  it('should merge route params and query manually', () => {
    const routeParameters = extractParameters('/quiz/:id', '/quiz/1')!;
    const query = extractQuery('level=2');

    expect({ ...routeParameters, ...query }).toEqual({
      id: '1',
      level: '2',
    });
  });
});
```

```ts
describe('Router navigation', () => {
  setupRouter();

  it('should push state when navigating via navigateTo', () => {
    router.navigateTo(TEST_URL.TRUE_FALSE_LEVEL_MODE_JUNIOR);

    expect(pushStateSpy).toHaveBeenCalled();
  });

  it('should redirect to not found on wrong url', () => {
    router.navigateTo('wrong-url');

    expect(location.pathname).toContain('/not-found');
  });

  it('should store params and query', () => {
    router.navigateTo(TEST_URL.TRUE_FALSE_LEVEL_MODE_MIDDLE);
    expect(router.getParams()).toEqual({
      id: '5',
      level: '5',
      mode: 'middle',
    });
  });

  it('should change params and query between navigations', () => {
    router.navigateTo(ROUTES.TRUE_FALSE_PAGE, { id: '1' }, { level: '2', mode: 'junior' });
    expect(router.getParams()).toEqual({
      id: '1',
      level: '2',
      mode: 'junior',
    });

    router.navigateTo(ROUTES.ABOUT_PAGE);
    expect(router.getParams()).toEqual({});

    router.navigateTo(TEST_URL.QUIZ_LEVEL);
    expect(router.getParams()).toEqual({
      id: '2',
      level: '1',
    });
  });

  it('should handle backward and forward history navigation', () => {
    router.navigateTo(TEST_URL.TRUE_FALSE_LEVEL_MODE_JUNIOR);

    expect(location.pathname).toContain('/true-false/1');
    expect(location.search).toBe('?level=2&mode=junior');
    expect(router.getParams()).toEqual({
      id: '1',
      level: '2',
      mode: 'junior',
    });

    router.navigateTo(TEST_URL.QUIZ_LEVEL);

    expect(location.pathname).toContain('/quiz/2');
    expect(location.search).toBe('?level=1');
    expect(router.getParams()).toEqual({
      id: '2',
      level: '1',
    });

    // Back navigation
    globalThis.dispatchEvent(
      new PopStateEvent('popstate', {
        state: { path: TEST_URL.TRUE_FALSE_LEVEL_MODE_JUNIOR },
      })
    );

    expect(router.getParams()).toEqual({
      id: '1',
      level: '2',
      mode: 'junior',
    });

    // Forward navigation
    globalThis.dispatchEvent(
      new PopStateEvent('popstate', {
        state: { path: TEST_URL.QUIZ_LEVEL },
      })
    );

    expect(router.getParams()).toEqual({
      id: '2',
      level: '1',
    });
  });
});
```

## Проблемы:

- Сложно тестировать навигацию без кнопок, решил добавить тесты.
- Для добавления тестов нужен отдельный конфиг файл и новые зависимости

## Планы:

- Планы на Week 2 разработка страницы Landing и About

## Затраченное время: 8 часов
