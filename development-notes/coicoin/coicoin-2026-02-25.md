# Дата: 2026-02-25

## Что было сделано:

- Доработал роутер, добавил replaceState, хранение state в history, useLocation hook. Теперь navigate() принимает цифры, строки, параметры, похож на navigate в React.
- Обновил документацию по роутеру `ROUTER.md`

## Проблемы:

- При переходе на страницу Авторизации лучше использовать replaceState, данного функционала у роутера не было, пришлось доработать. Так же было добавлено много других полезных функций.

## Решения:

- Добавил replaceState для перезаписи истории. Функция navigate() принимает state, replace.
- Добавил возможность перемещаться назад например на странице Not Found кнопка "вернуться назад", работает аналогично history(-1), только navigate(-1).
- Добавил возможность хранение состояния state в history.

## Планы:

- Планы на 2 Sprint - разработка страницы Landing и Quiz

## Затраченное время: 5 часов
