# Дата: 2026-03-12

## Что было сделано

- Провел код ревью `https://github.com/FierceSloth/rss-tandem-app/pull/39`, пробежался по коду, прочитал:
  1. Recomended to use mixins instead of

  ```
  @include media-tablet {
      bottom: calc(16px + env(safe-area-inset-bottom));
      left: 16px;
      right: 16px;
      align-items: stretch;
  }
  ```

  2. Recomended to use `let container = document.getElementById(TOAST_CONTAINER_ID);` instead of `let container = document.querySelector(`#${TOAST_CONTAINER_ID}`);`

- Продолжаю работать с Quiz Page.

## Проблемы

- Как передавать пропсы в другие компоненты Quiz page, сложность обмена данными между компонентами.
- Разработал таймер, сначала сделал `setInterval`, потом почитал что он не точный и рекомендуется использовать `requestAnimationFrame`.
- Заметил что у меея была ошибка в дневнике и все исправил. Ошибка опечатки заключалась в том, что например для `# Дата: 2026-03-08` писал февральский месяц 02, поэтому результат анализа моей активности на курсе был искажен.

## Решения

- Перешел на `requestAnimationFrame`. Создал отдельный TimerEngine для этого.
- Исправил даты в дневнике.

## Планы

- Продолжить работу над страницей `Quiz` в рамках 4 спринта.
- К концу спринта начать работать с True/False страницей.

## Затраченное время

8 часов
